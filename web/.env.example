## 前端示例环境变量（复制为 web/.env.local 使用；请勿提交包含敏感信息的文件）
##
## 后端地址（必须显式配置；建议生产使用 HTTPS 域名，例如 https://api.example.com）
VITE_API_BASE_URL=http://localhost:8080
VITE_API_ZERO_URL=http://localhost:8081
##
## OAuth ClientId（不是密钥，但也建议按环境分别配置）
VITE_GITHUB_CLIENT_ID=
VITE_TWITTER_CLIENT_ID=
